---
deployment:
  tasks:
    - export DEPLOYPATH=/home/grupaqgl/bills.gridbase.com.do/
    - /usr/bin/rsync -rlpt --exclude='.git' --exclude='.cpanel.yml' --exclude='legacy_*' ./ $DEPLOYPATH
    - /usr/local/bin/php $DEPLOYPATH/artisan view:clear --no-interaction
