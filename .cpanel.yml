---
deployment:
  tasks:
    - export DEPLOYPATH=/home/grupaqgl/bills.gridbase.com.do/
    - /usr/bin/rsync -rlpt --exclude='.git' --exclude='.cpanel.yml' --exclude='vendor' --exclude='.env' --exclude='storage' ./ $DEPLOYPATH
